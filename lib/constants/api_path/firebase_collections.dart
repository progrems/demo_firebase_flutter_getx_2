// Nom des collections Firestore. On le centralise pour éviter les fautes
// comme 'Users', 'user' ou 'utilisateurs' dans différents fichiers.
abstract class FirebaseCollections {
  static const users = 'users';
}
